import 'package:flutter/widgets.dart';

class QuestionBinder extends StatelessWidget {
  const QuestionBinder({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
