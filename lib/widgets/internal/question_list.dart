import 'package:flutter/material.dart';

class QeustionList extends StatelessWidget {
  const QeustionList({super.key});

  @override
  Widget build(BuildContext context) {
    final titleList = _buildTitleList(context);
    final widgetList = _buildWidgetList(context);
    // TODO: implement build
    throw UnimplementedError();
  }

  Widget _buildWidgetList(BuildContext context) {
    throw UnimplementedError();
  }

  Widget _buildTitleList(BuildContext context) {
    throw UnimplementedError();
  }
}
